# For local development, you can create a .env.local file with this content.
# The MONGODB_URI is only needed if you want to connect to a real database.
# Otherwise, the app will use in-memory demo data.
MONGODB_URI="your-mongodb-connection-string-here"
